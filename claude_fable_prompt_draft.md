# Claude Fable 입력용 프롬프트

> 콘텐츠는 전부 예시/빈칸([ ] 표시)으로 채워둔 틀입니다. 실제 정보가 준비되면 [ ] 부분만 교체해서 넣으면 됩니다.

---

## 1. 프로젝트 개요

나는 GIST 컴퓨터공학과 3학년에 재학 중이면서 동시에 힙합 베이스의 전자음악(디지털 음악) 아티스트로 활동하는 "wani"다. 개발자로서의 나와 아티스트로서의 나, 두 자아를 하나의 정적 웹사이트에 담은 개인 포트폴리오를 만들어줘. 나는 디지털이라는 세계 자체를 사랑하고 맥시멀리즘을 선호하는 사람이라, 보는 사람이 압도당할 정도로 현대적이고 디지털적인 밀도 있는 디자인과 인터랙션을 원한다.

- 배포 대상: GitHub Pages (순수 정적 파일: HTML/CSS/JS, 빌드 도구 없이 바로 배포 가능해야 함. 만약 프레임워크를 쓴다면 GitHub Actions로 빌드해 `gh-pages` 브랜치에 배포하는 구조까지 포함)
- 반응형 디자인 필수 (모바일/데스크톱 모두 대응)
- 성능: 인터랙션이 화려하더라도 로딩/스크롤 성능 저하가 없도록 최적화 (지연 로딩, requestAnimationFrame 사용 등)

## 2. 톤 앤 매너

나는 디지털 세계 자체를 사랑하는 사람이다. 전공(컴퓨터공학)도, 음악(디지털 음악)도 모두 "디지털"이라는 정체성으로 연결돼 있고, 이 정체성이 사이트 전체에 과감하게 드러나야 한다. 전체적인 비주얼 톤은 **맥시멀리즘 기반의 압도적인 디지털/사이버 감성 + 실험적 글리치·노이즈**다. 미니멀리즘은 지양한다.

- 화면을 정보·그래픽·모션으로 가득 채우는 맥시멀 레이아웃. 배경 노이즈, 파티클, 데이터 시각화, 타이포그래피가 동시에 겹겹이 존재하는 레이어드 구성
- 코드 스니펫, 오디오 파형, 스펙트로그램, 터미널/HUD UI 같은 디지털 네이티브 그래픽 모티프를 적극 차용
- 방문자가 정보량과 움직임에 "압도당하는" 느낌을 받아야 하되, 혼란스럽지 않도록 스크롤 진행에 따른 시선 흐름·포커스는 명확하게 설계 (맥시멀하지만 방향성은 있어야 함)
- 컬러: 블랙 배경 위 강렬한 멀티 네온 컬러 동시 사용(예: 시안·마젠타·라임), 그라디언트와 크로마틱 애버레이션 적극 활용
- 타이포그래피: 서로 다른 폰트 웨이트·크기를 동시에 배치한 밀도 높은 타이포 콜라주, 모노스페이스+글리치 텍스트 혼합
- RGB 채널 분리, 스캔라인, VHS 노이즈, 디지털 아티팩트 같은 시각 언어를 레이어 곳곳에 사용
- 참고 레퍼런스 사이트/무드보드가 있다면: [ 링크 또는 설명 추가 ]

## 3. 핵심 인터랙션

사이트의 개성을 결정짓는 시그니처 인터랙션은 다음을 겹겹이 조합해 "압도적"인 밀도를 만든다.

1. **오디오 반응형 비주얼(다중 레이어)**: 파티클 시스템 + 웨이브폼 + 스펙트럼 바 등 여러 시각 레이어가 동시에 실제 재생 중인 음악(SoundCloud/YouTube 임베드 오디오 또는 별도 오디오 파일)의 진폭에 반응해 움직인다.
2. **커서/스크롤 반응형 글리치(다중 레이어 패럴랙스)**: 마우스 움직임과 스크롤에 따라 텍스트·이미지에 RGB 분리, 지터(jitter), 트레일 이펙트, 스캔라인 노이즈가 실시간으로 반응하며, 여러 요소가 서로 다른 속도로 움직이는 복합 패럴랙스를 구성한다.
3. **살아있는 제너레이티브 배경**: 코드/데이터가 흐르는 느낌의 매트릭스 스타일 또는 파티클 필드가 배경에서 끊임없이 움직인다.
4. **디지털 로딩/전환 연출**: 페이지 로딩이나 두 모드 간 전환 시 ASCII 아트, 터미널 부팅 시퀀스 같은 화려한 디지털 트랜지션(글리치 트랜지션)을 연출한다.

## 4. 정보 구조: 두 모드 전환

사이트는 하나의 진입점에서 **"개발자 wani"** / **"아티스트 wani"** 두 모드로 전환되는 구조다. 방문자는 랜딩 화면에서 두 모드 중 하나를 선택(또는 토글)해 들어가고, 언제든 전환할 수 있는 버튼/제스처가 있어야 한다.

- 두 모드는 각각 다른 컬러 악센트/분위기를 가질 수 있음 (단, 전체 글리치/노이즈 톤은 공유)
- 아티스트 모드에는 음악 활동뿐 아니라 "기타 활동"(뮤직비디오 제작, 팟캐스트 제작, 장학재단 활동)도 함께 포함한다.

### 4-1. 개발자 wani 모드

담고 싶은 내용:
- 소개: [ 예: GIST 컴퓨터공학과 3학년, AI·오디오 신호처리 연구 지향 ]
- 학점교류: KAIST 학점교류 [ 시기 / 수강 내용 / 배운 점 ], UC Berkeley 학점교류 [ 시기 / 수강 내용 / 배운 점 ]
- Google Machine Learning Bootcamp 참여 [ 시기 / 내용 / 결과물 ]
- 창업동아리 활동 [ 동아리명 / 역할 / 한 일 ]
- 기타 개인/연구 프로젝트 [ 프로젝트명 / 설명 / 기술스택 / 링크 ] (여러 개 가능)
- 외부 링크: GitHub [ URL ], LinkedIn [ URL ]

### 4-2. 아티스트 wani 모드

담고 싶은 내용:
- 소개: [ 예: 힙합 베이스의 전자음악을 하는 아티스트 wani. 장르에 국한되지 않고 자신을 표현 ]
- 소속 팀/크루: [ 팀명 / 역할 / 소개 ]
- 발매 앨범/EP: [ 제목 / 연도 / 링크 ] (여러 개 가능)
- 공연 경력: [ 공연명 또는 장소 / 연도 / 규모·역할 ] (여러 개 가능)
- 외부 링크: SoundCloud [ URL ], YouTube [ URL ]
- 기타 활동:
  - 뮤직비디오 제작 프로젝트 [ 곡명 / 역할 / 설명 / 링크 ]
  - 팟캐스트 제작 프로젝트 [ 주제 / 역할 / 설명 / 링크 ]
  - DB김준기문화재단 장학활동 [ 기간 / 내용 ]

## 5. 기술 스펙 요청

- 순수 HTML/CSS/JS(바닐라) 또는 가벼운 정적 사이트 생성기 기준으로 작성 (React 등 무거운 프레임워크 지양, GitHub Pages 배포 단순성 우선)
- 오디오 반응형 비주얼은 Web Audio API 기반으로 구현
- 다중 레이어 글리치·파티클 효과는 Canvas/WebGL 기반으로 구현하되, 맥시멀한 밀도에도 불구하고 프레임 드랍이 없도록 파티클 수·레이어 렌더링을 최적화(오프스크린 컬링, requestAnimationFrame 스로틀링 등)
- 저사양 기기 대응: 성능 옵션(이펙트 강도 낮추기, 레이어 일부 비활성화) 제공
- 접근성: 모션에 민감한 사용자를 위한 "reduce motion" 대응 옵션 포함 (압도적인 연출과 별개로 반드시 제공)
- 코드에 주석을 달아 추후 내가 직접 콘텐츠를 교체하기 쉽게 구성

---

**요청**: 위 내용을 반영해서 실제 동작하는 정적 웹사이트 코드 전체(HTML/CSS/JS 파일 구조)를 만들어줘. [ ] 표시된 부분은 현재 예시/빈칸이니, 실제 콘텐츠가 들어갈 자리라는 걸 감안해서 구조와 인터랙션을 우선 완성해줘.
